/**
 * Site-wide constants. Single-tenant board for residence 502, so values that
 * are the same for every product (e.g. handover location) live here rather
 * than as duplicated per-product columns.
 */

export const SITE_HANDOVER_TEXT = "玄関先での受け渡し（502号室）";
